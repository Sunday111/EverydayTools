#pragma once

namespace edt::geom
{
    template<typename T, size_t Rows, size_t Columns>
    class Matrix;

    template<typename T, size_t Rows, size_t Columns>
    class MatrixView;

    template<typename T, size_t Rows, size_t Columns>
    class LineMatrixView;
}

